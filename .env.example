# Cloudflare Workers 환경변수 예시
# 실제 사용 시 이 파일을 복사하여 .env로 저장하고 실제 값을 입력하세요.
# .env 파일은 .gitignore에 포함되어 있으므로 Git에 커밋되지 않습니다.

# ====================================
# Airtable API 설정
# ====================================

# Airtable Personal Access Token
# 발급 방법: https://airtable.com/create/tokens
# Scopes: data.records:read, data.records:write
AIRTABLE_API_KEY=your_personal_access_token_here

# Airtable Base ID
# Base URL에서 확인: https://airtable.com/appXXXXXXXXXXXXXX/...
# 예: appXXXXXXXXXXXXXX
AIRTABLE_BASE_ID=your_base_id_here

# Airtable Table 이름
# 예: Leads
AIRTABLE_TABLE_NAME=Leads

# ====================================
# CORS 설정
# ====================================

# 허용된 Origins (쉼표로 구분)
# 아임웹 사이트 URL을 입력하세요.
# 예: https://site1.imweb.me,https://site2.imweb.me
# 모든 Origin 허용 시: *
ALLOWED_ORIGINS=https://your-site.imweb.me

# ====================================
# Cloudflare Workers 설정 방법
# ====================================

# 1. Wrangler CLI 설치
#    npm install -g wrangler

# 2. Cloudflare 로그인
#    wrangler login

# 3. Secrets 설정 (환경변수를 안전하게 저장)
#    wrangler secret put AIRTABLE_API_KEY
#    wrangler secret put AIRTABLE_BASE_ID
#    wrangler secret put AIRTABLE_TABLE_NAME
#    wrangler secret put ALLOWED_ORIGINS

# 4. Worker 배포
#    wrangler deploy

# 5. 배포 URL 확인
#    배포 후 출력되는 URL을 복사하여 Window.CONFIG.form.airtable.workerUrl에 설정
#    예: https://100zoad-airtable-proxy.your-subdomain.workers.dev/submit
